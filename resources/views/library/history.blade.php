@extends('layouts.app')
@section('title','Lịch sử nghe')
@section('content')<h1 class="section-title mb-1">Lịch sử nghe</h1><p class="text-muted mb-4">Các bài hát gần đây của bạn.</p><div class="glass-card p-3" data-song-collection>@forelse($histories as $history)@if($history->song)<div class="song-row"><button class="border-0 bg-transparent p-0" data-play-song data-song='@json($history->song->toPlayerPayload())'><img src="{{ $history->song->cover_url }}" class="song-row-cover"></button><div class="min-w-0"><a class="fw-bold d-block text-truncate" href="{{ route('songs.show',$history->song) }}">{{ $history->song->title }}</a><small class="text-muted">{{ $history->song->user->name }}</small></div><div class="optional text-muted">{{ $history->song->genre?->name }}</div><div class="optional text-muted">Dừng ở {{ intdiv($history->last_position_seconds,60) }}:{{ str_pad($history->last_position_seconds%60,2,'0',STR_PAD_LEFT) }}</div><small class="text-muted">{{ $history->listened_at->diffForHumans() }}</small></div>@endif @empty<div class="text-center p-5 text-muted">Chưa có lịch sử nghe.</div>@endforelse</div><div class="mt-4">{{ $histories->links() }}</div>@endsection
