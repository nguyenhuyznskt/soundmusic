@extends('layouts.app')
@section('title','Báo cáo vi phạm')
@section('bodyClass','admin-layout')
@section('content')
<div class="d-flex flex-wrap justify-content-between align-items-end gap-3 mb-4"><div><h1 class="section-title">Báo cáo vi phạm</h1><p class="text-muted mb-0">Xử lý nội dung được cộng đồng báo cáo.</p></div><a class="btn btn-soft" href="{{ route('admin.dashboard') }}">Dashboard</a></div>
<form class="mb-3" method="get"><select class="form-select" name="status" onchange="this.form.submit()" style="max-width:220px"><option value="">Tất cả trạng thái</option>@foreach(['open','reviewing','resolved','dismissed'] as $status)<option value="{{ $status }}" @selected(request('status')===$status)>{{ $status }}</option>@endforeach</select></form>
<div class="row g-3">@forelse($reports as $report)<div class="col-12"><div class="glass-card p-4"><div class="d-flex flex-wrap justify-content-between gap-3"><div><div class="d-flex gap-2 mb-2"><span class="badge text-bg-danger">{{ $report->reason }}</span><span class="badge text-bg-dark">{{ $report->status }}</span></div><h2 class="h5">{{ class_basename($report->reportable_type) }} #{{ $report->reportable_id }}</h2><p class="text-muted mb-2">Người báo cáo: {{ $report->user->name }} · {{ $report->created_at->diffForHumans() }}</p><p class="mb-0">{{ $report->details ?: 'Không có mô tả thêm.' }}</p></div><form method="post" action="{{ route('admin.reports.resolve',$report) }}" style="min-width:300px">@csrf @method('put')<select class="form-select mb-2" name="status">@foreach(['reviewing','resolved','dismissed'] as $status)<option value="{{ $status }}" @selected($report->status===$status)>{{ $status }}</option>@endforeach</select><textarea class="form-control mb-2" name="admin_note" rows="2" placeholder="Ghi chú xử lý">{{ $report->admin_note }}</textarea><button class="btn btn-accent w-100">Cập nhật</button></form></div></div></div>@empty<div class="col-12"><div class="glass-card p-5 text-center text-muted">Không có báo cáo.</div></div>@endforelse</div><div class="mt-4">{{ $reports->links() }}</div>
@endsection
