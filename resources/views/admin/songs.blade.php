@extends('layouts.app')
@section('title','Kiểm duyệt bài hát')
@section('bodyClass','admin-layout')
@section('content')
<div class="d-flex flex-wrap justify-content-between align-items-end gap-3 mb-4"><div><h1 class="section-title">Kiểm duyệt bài hát</h1><p class="text-muted mb-0">Duyệt, từ chối hoặc chặn nội dung.</p></div><div class="d-flex gap-2"><a class="btn btn-soft" href="{{ route('admin.dashboard') }}">Dashboard</a><a class="btn btn-soft" href="{{ route('admin.users.index') }}">Người dùng</a><a class="btn btn-soft" href="{{ route('admin.genres.index') }}">Thể loại</a></div></div>
<form class="glass-card p-3 mb-3" method="get"><div class="row g-2"><div class="col-md-8"><input class="form-control" name="q" value="{{ request('q') }}" placeholder="Tìm bài hát hoặc nghệ sĩ..."></div><div class="col-md-2"><select class="form-select" name="status"><option value="">Tất cả</option>@foreach(['pending','published','rejected','blocked'] as $status)<option value="{{ $status }}" @selected(request('status')===$status)>{{ $status }}</option>@endforeach</select></div><div class="col-md-2"><button class="btn btn-accent w-100">Lọc</button></div></div></form>
<div class="glass-card p-3"><div class="table-responsive"><table class="table table-dark align-middle mb-0"><thead><tr><th>Bài hát</th><th>Nghệ sĩ</th><th>Thể loại</th><th>Lượt nghe</th><th>Kiểm duyệt</th></tr></thead><tbody>@foreach($songs as $song)<tr><td><div class="d-flex gap-3 align-items-center"><img src="{{ $song->cover_url }}" width="52" height="52" class="rounded object-fit-cover"><div><a class="fw-bold" href="{{ route('songs.show',$song) }}">{{ $song->title }}</a><small class="d-block text-muted">{{ $song->created_at->format('d/m/Y H:i') }}</small></div></div></td><td>{{ $song->user->name }}</td><td>{{ $song->genre?->name ?? '—' }}</td><td>{{ number_format($song->play_count) }}</td><td><form class="d-flex gap-2" method="post" action="{{ route('admin.songs.moderate',$song) }}">@csrf @method('put')<select class="form-select form-select-sm" name="status">@foreach(['pending','published','rejected','blocked'] as $status)<option value="{{ $status }}" @selected($song->status===$status)>{{ $status }}</option>@endforeach</select><button class="btn btn-sm btn-accent">Lưu</button></form></td></tr>@endforeach</tbody></table></div></div><div class="mt-4">{{ $songs->links() }}</div>
@endsection
