@extends('layouts.app')
@section('title','Quản lý người dùng')
@section('bodyClass','admin-layout')
@section('content')
<div class="d-flex flex-wrap justify-content-between align-items-end gap-3 mb-4"><div><h1 class="section-title">Người dùng</h1><p class="text-muted mb-0">Phân quyền và khóa/mở tài khoản.</p></div><div class="d-flex gap-2"><a class="btn btn-soft" href="{{ route('admin.dashboard') }}">Dashboard</a><a class="btn btn-soft" href="{{ route('admin.songs.index') }}">Bài hát</a><a class="btn btn-soft" href="{{ route('admin.reports.index') }}">Báo cáo</a></div></div>
<form class="glass-card p-3 mb-3" method="get"><div class="row g-2"><div class="col-md-7"><input class="form-control" name="q" value="{{ request('q') }}" placeholder="Tìm tên, email, username..."></div><div class="col-md-3"><select class="form-select" name="role"><option value="">Tất cả vai trò</option>@foreach(['listener','artist','admin'] as $role)<option value="{{ $role }}" @selected(request('role')===$role)>{{ $role }}</option>@endforeach</select></div><div class="col-md-2"><button class="btn btn-accent w-100">Lọc</button></div></div></form>
<div class="glass-card p-3"><div class="table-responsive"><table class="table table-dark align-middle mb-0"><thead><tr><th>Người dùng</th><th>Vai trò</th><th>Trạng thái</th><th>Ngày tạo</th><th>Cập nhật</th></tr></thead><tbody>@foreach($users as $user)<tr><td><div class="d-flex gap-3 align-items-center"><span class="avatar">{{ $user->initials }}</span><div><strong>{{ $user->name }}</strong><small class="d-block text-muted">{{ $user->email }} · {{ '@'.$user->username }}</small></div></div></td><td colspan="2"><form class="d-flex gap-2 align-items-center" method="post" action="{{ route('admin.users.update',$user) }}">@csrf @method('put')<select class="form-select form-select-sm" name="role" style="width:130px">@foreach(['listener','artist','admin'] as $role)<option value="{{ $role }}" @selected($user->role===$role)>{{ $role }}</option>@endforeach</select><select class="form-select form-select-sm" name="is_active" style="width:130px"><option value="1" @selected($user->is_active)>Hoạt động</option><option value="0" @selected(!$user->is_active)>Bị khóa</option></select></td><td>{{ $user->created_at->format('d/m/Y') }}</td><td><button class="btn btn-sm btn-accent">Lưu</button></form></td></tr>@endforeach</tbody></table></div></div><div class="mt-4">{{ $users->links() }}</div>
@endsection
