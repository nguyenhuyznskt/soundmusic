@extends('layouts.app')
@section('title','Chỉnh sửa hồ sơ')
@section('content')<div class="row justify-content-center"><div class="col-lg-8"><h1 class="section-title mb-4">Chỉnh sửa hồ sơ</h1><form class="glass-card p-4 p-lg-5 mb-4" method="post" enctype="multipart/form-data" action="{{ route('profiles.update') }}">@csrf @method('put')<div class="row g-4"><div class="col-md-4 text-center"><span class="avatar avatar-lg mx-auto mb-3">@if($user->avatar_url)<img id="avatarPreview" src="{{ $user->avatar_url }}" class="w-100 h-100 rounded-circle object-fit-cover">@else<span id="avatarText">{{ $user->initials }}</span><img id="avatarPreview" class="w-100 h-100 rounded-circle object-fit-cover d-none">@endif</span><input class="form-control" type="file" name="avatar" accept="image/jpeg,image/png,image/webp" onchange="avatarPreview.src=URL.createObjectURL(this.files[0]);avatarPreview.classList.remove('d-none');document.getElementById('avatarText')?.classList.add('d-none')"></div><div class="col-md-8"><div class="mb-3"><label class="form-label">Tên hiển thị</label><input class="form-control" name="name" value="{{ old('name',$user->name) }}" required></div><div class="mb-3"><label class="form-label">Tên đăng nhập</label><input class="form-control" value="{{ $user->username }}" disabled></div><div class="mb-4"><label class="form-label">Giới thiệu</label><textarea class="form-control" name="bio" rows="6" maxlength="1500">{{ old('bio',$user->bio) }}</textarea></div><button class="btn btn-accent px-4">Lưu hồ sơ</button></div></div></form><form class="glass-card p-4 p-lg-5" method="post" action="{{ route('profiles.password') }}">@csrf @method('put')<h2 class="h5 fw-bold mb-3">Đổi mật khẩu</h2><div class="mb-3"><label class="form-label">Mật khẩu hiện tại</label><input class="form-control" type="password" name="current_password" required></div><div class="row g-3"><div class="col-md-6"><label class="form-label">Mật khẩu mới</label><input class="form-control" type="password" name="password" required></div><div class="col-md-6"><label class="form-label">Nhập lại mật khẩu</label><input class="form-control" type="password" name="password_confirmation" required></div></div><button class="btn btn-accent mt-3">Đổi mật khẩu</button></form></div></div>@endsection
