@extends('layouts.app')
@section('title','Kho nhạc của tôi')
@section('content')
<div class="d-flex justify-content-between align-items-end mb-4"><div><h1 class="section-title">Kho nhạc của tôi</h1><p class="text-muted mb-0">Quản lý bài hát và trạng thái kiểm duyệt.</p></div><a class="btn btn-accent" href="{{ route('songs.create') }}"><i class="bi bi-plus-lg me-1"></i>Tải bài hát</a></div>
<div class="glass-card p-3"><div class="table-responsive"><table class="table table-dark align-middle mb-0"><thead><tr><th>Bài hát</th><th>Thể loại</th><th>Trạng thái</th><th>Lượt nghe</th><th>Ngày tạo</th><th></th></tr></thead><tbody>@forelse($songs as $song)<tr><td><div class="d-flex align-items-center gap-3"><img src="{{ $song->cover_url }}" width="54" height="54" class="rounded object-fit-cover"><div><a class="fw-bold" href="{{ route('songs.show',$song) }}">{{ $song->title }}</a><small class="d-block text-muted">{{ $song->formatted_duration }}</small></div></div></td><td>{{ $song->genre?->name ?? '—' }}</td><td><span class="badge rounded-pill text-bg-{{ $song->status==='published'?'success':($song->status==='rejected'?'danger':'warning') }}">{{ $song->status }}</span></td><td>{{ number_format($song->play_count) }}</td><td>{{ $song->created_at->format('d/m/Y') }}</td><td><div class="d-flex gap-1 justify-content-end"><a class="btn btn-sm btn-soft" href="{{ route('songs.edit',$song) }}"><i class="bi bi-pencil"></i></a><form method="post" action="{{ route('songs.destroy',$song) }}" data-confirm="Xóa vĩnh viễn bài hát này?">@csrf @method('delete')<button class="btn btn-sm btn-outline-danger"><i class="bi bi-trash"></i></button></form></div></td></tr>@empty<tr><td colspan="6" class="text-center py-5 text-muted">Bạn chưa tải bài hát nào.</td></tr>@endforelse</tbody></table></div></div><div class="mt-4">{{ $songs->links() }}</div>
@endsection
