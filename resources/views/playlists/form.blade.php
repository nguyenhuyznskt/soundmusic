@extends('layouts.app')
@section('title',$editing?'Sửa playlist':'Tạo playlist')
@section('content')
<div class="row justify-content-center"><div class="col-lg-8"><h1 class="section-title mb-4">{{ $editing?'Chỉnh sửa playlist':'Tạo playlist mới' }}</h1><form class="glass-card p-4 p-lg-5" method="post" enctype="multipart/form-data" action="{{ $editing?route('playlists.update',$playlist):route('playlists.store') }}">@csrf @if($editing)@method('put')@endif<div class="row g-4"><div class="col-md-4"><div class="music-cover-wrap mb-3"><img id="coverPreview" src="{{ $playlist->cover_url }}" class="music-cover"></div><input class="form-control" type="file" name="cover" accept="image/jpeg,image/png,image/webp" onchange="coverPreview.src=URL.createObjectURL(this.files[0])"></div><div class="col-md-8"><div class="mb-3"><label class="form-label">Tên playlist</label><input class="form-control form-control-lg" name="name" value="{{ old('name',$playlist->name) }}" required></div><div class="mb-3"><label class="form-label">Mô tả</label><textarea class="form-control" name="description" rows="5">{{ old('description',$playlist->description) }}</textarea></div><div class="mb-4"><label class="form-label">Quyền riêng tư</label><select class="form-select" name="visibility"><option value="public" @selected(old('visibility',$playlist->visibility)==='public')>Công khai</option><option value="private" @selected(old('visibility',$playlist->visibility)==='private')>Riêng tư</option></select></div><div class="d-flex justify-content-end gap-2"><a class="btn btn-soft" href="{{ $editing?route('playlists.show',$playlist):route('playlists.index') }}">Hủy</a><button class="btn btn-accent">{{ $editing?'Lưu thay đổi':'Tạo playlist' }}</button></div></div></div></form></div></div>
@endsection
